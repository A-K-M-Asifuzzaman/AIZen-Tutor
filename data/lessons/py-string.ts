import type { Lesson } from "../types"

export const PYTHON_STRING: Lesson = {
  id: "python-string",
  title: "String",
  category: "Python",
  content: [
    {
      heading: "What are Strings?",
      body: "Strings are sequences of characters used to represent text in Python. They are immutable, meaning once created, they cannot be changed. Python treats single quotes and double quotes the same way.",
      code: "# String creation\nname = 'Alice'           # Single quotes\ncity = \"New York\"        # Double quotes\nmulti_line = '''This is\na multi-line\nstring'''                 # Triple quotes for multi-line\n\n# Empty string\nempty = \"\"\n\n# String with quotes\nquote = \"He said, 'Hello!'\"\nquote2 = 'She said, \"Hi!\"'\n\n# String concatenation\nfirst_name = \"John\"\nlast_name = \"Doe\"\nfull_name = first_name + \" \" + last_name\nprint(full_name)  # John Doe\n\n# String repetition\nprint(\"Ha\" * 3)  # HaHaHa\n\n# String length\nmessage = \"Hello World\"\nprint(len(message))  # 11 (space counts as a character)",
      note: "Strings are immutable in Python. Any operation that modifies a string creates a new string object."
    },
    {
      heading: "String Indexing and Slicing",
      body: "Strings support indexing to access individual characters and slicing to extract substrings. Python uses 0-based indexing.",
      code: "# String indexing\ntext = \"Python\"\nprint(text[0])   # P (first character)\nprint(text[1])   # y\nprint(text[-1])  # n (last character)\nprint(text[-2])  # o (second to last)\n\n# String slicing - syntax: [start:end:step]\ntext = \"Hello, World!\"\n\nprint(text[0:5])     # Hello (characters 0 to 4)\nprint(text[7:])      # World! (from index 7 to end)\nprint(text[:5])      # Hello (from start to index 4)\nprint(text[::2])     # Hlo ol! (every 2nd character)\nprint(text[::-1])    # !dlroW ,olleH (reverse string)\n\n# More slicing examples\nword = \"Programming\"\nprint(word[0:4])     # Prog\nprint(word[4:])      # ramming\nprint(word[-4:])     # ming (last 4 characters)\nprint(word[2:8:2])   # oga (characters at positions 2,4,6)\n\n# Slicing with negative indices\nprint(word[-6:-2])   # ammi (from -6 to -3)\nprint(word[-3:])     # ing (last 3 characters)",
      note: "Slicing creates a new string. The start index is inclusive, the end index is exclusive. Omitting start or end uses the beginning or end of the string."
    },
    {
      heading: "String Methods - Basic Operations",
      body: "Python provides numerous built-in methods for string manipulation. Here are the most commonly used ones.",
      code: "# Case conversion\nname = \"hello world\"\nprint(name.upper())      # HELLO WORLD\nprint(name.lower())      # hello world\nprint(name.capitalize()) # Hello world\nprint(name.title())      # Hello World\nprint(name.swapcase())   # HELLO WORLD\n\n# Checking string content\ns = \"Hello123\"\nprint(s.isalpha())       # False (contains numbers)\nprint(s.isalnum())       # True (letters and numbers)\nprint(s.isdigit())       # False (not all digits)\nprint(s.islower())       # False (has uppercase)\nprint(s.isupper())       # False\nprint(s.isspace())       # False\n\n# Finding and replacing\nmessage = \"Hello Hello Hello\"\nprint(message.count(\"Hello\"))  # 3\nprint(message.find(\"l\"))       # 2 (first occurrence)\nprint(message.rfind(\"l\"))      # 15 (last occurrence)\nprint(message.index(\"o\"))      # 4 (raises error if not found)\n\n# Replacement\nprint(message.replace(\"Hello\", \"Hi\"))  # Hi Hi Hi\nprint(message.replace(\"l\", \"x\", 2))    # Hexxo Hello Hello\n\n# Stripping whitespace\nmessy = \"  Hello World  \\n\"\nprint(messy.strip())     # \"Hello World\" (removes both ends)\nprint(messy.lstrip())    # \"Hello World  \\n\" (left only)\nprint(messy.rstrip())    # \"  Hello World\" (right only)\n\n# Splitting and joining\ndata = \"apple,banana,orange\"\nprint(data.split(\",\"))   # ['apple', 'banana', 'orange']\n\nwords = [\"Hello\", \"World\"]\nprint(\" \".join(words))   # Hello World\nprint(\"-\".join(words))   # Hello-World",
      note: "String methods return new strings since strings are immutable. Methods like find() return -1 if substring is not found, while index() raises a ValueError."
    },
    {
      heading: "String Formatting",
      body: "Python offers multiple ways to format strings. Choose the method that best fits your needs and Python version.",
      code: "# Method 1: f-strings (Python 3.6+) - Recommended\nname = \"Alice\"\nage = 25\nheight = 5.6\nprint(f\"Name: {name}, Age: {age}, Height: {height}\")\nprint(f\"Next year you'll be {age + 1} years old\")\nprint(f\"Height in meters: {height * 0.3048:.2f}\")  # Format with 2 decimals\n\n# Method 2: format() method\nprint(\"Name: {}, Age: {}\".format(name, age))\nprint(\"Name: {0}, Age: {1}\".format(name, age))  # Positional\nprint(\"Name: {n}, Age: {a}\".format(n=name, a=age))  # Named\n\n# Method 3: % formatting (older style)\nprint(\"Name: %s, Age: %d\" % (name, age))\nprint(\"Height: %.2f\" % height)\n\n# Advanced f-string formatting\nprice = 49.99\nquantity = 3\nprint(f\"Total: ${price * quantity:.2f}\")\nprint(f\"{{Brackets}}\")  # Escaping curly braces\n\n# Formatting with alignment\nname = \"Alice\"\nprint(f\"{name:>10}\")    # Right align (10 width)\nprint(f\"{name:<10}\")    # Left align\nprint(f\"{name:^10}\")    # Center align\n\n# Number formatting\nnumber = 1234567.89\nprint(f\"{number:,.2f}\")  # 1,234,567.89 (with commas)\nprint(f\"{number:.2e}\")   # 1.23e+06 (scientific notation)\nprint(f\"{number:>15,.2f}\")  # Right align with formatting",
      note: "f-strings are the most readable and performant way to format strings in modern Python. They allow expressions inside the curly braces."
    },
    {
      heading: "String Search and Validation",
      body: "Python provides methods to search for substrings and validate string content, essential for text processing and user input validation.",
      code: "# Checking prefix/suffix\nfilename = \"document.pdf\"\nprint(filename.startswith(\"doc\"))   # True\nprint(filename.endswith(\".pdf\"))    # True\nprint(filename.endswith(\".txt\"))    # False\n\n# Advanced search methods\ntext = \"The quick brown fox jumps over the lazy dog\"\nprint(\"fox\" in text)           # True (membership operator)\nprint(\"cat\" in text)           # False\nprint(text.find(\"fox\"))        # 16 (position of first character)\nprint(text.rfind(\"the\"))       # 31 (search from right)\nprint(text.index(\"fox\"))       # 16 (similar to find but raises error)\n\n# Count occurrences\nprint(text.count(\"o\"))         # 4\nprint(text.count(\"the\"))       # 2 (case sensitive)\n\n# String validation methods\nuser_input = \"abc123\"\nprint(user_input.isalnum())    # True (alphanumeric)\nprint(user_input.isalpha())    # False (has numbers)\nprint(user_input.isdigit())    # False\nprint(user_input.isnumeric())  # False\nprint(user_input.isdecimal())  # False\n\n# Check string content\ndef validate_password(password):\n    if len(password) < 8:\n        return \"Password too short\"\n    if not any(c.isupper() for c in password):\n        return \"Need at least one uppercase letter\"\n    if not any(c.islower() for c in password):\n        return \"Need at least one lowercase letter\"\n    if not any(c.isdigit() for c in password):\n        return \"Need at least one number\"\n    return \"Valid password\"\n\nprint(validate_password(\"Abc123!\"))    # Password too short\nprint(validate_password(\"Abcdefgh!\"))  # Need at least one number\nprint(validate_password(\"Abc12345!\"))  # Valid password",
      note: "Use startswith() and endswith() for checking file extensions, prefixes in filenames, or validating input formats. The 'in' operator is the most Pythonic way to check for substring existence."
    },
    {
      heading: "Advanced String Methods",
      body: "These advanced string methods handle common text processing tasks like padding, character manipulation, and substring operations.",
      code: "# Padding and alignment\ntext = \"Python\"\nprint(text.center(20, \"*\"))    # *******Python*******\nprint(text.ljust(15, \"-\"))     # Python----------\nprint(text.rjust(15, \"-\"))     # ----------Python\nprint(text.zfill(10))           # 0000Python (pad with zeros)\n\n# Character manipulation\nprint(ord('A'))   # 65 (ASCII value)\nprint(chr(65))    # 'A' (character from ASCII)\nprint(ord('a'))   # 97\n\n# Expanding tabs\ntext = \"Hello\\tWorld\"\nprint(text)                # Hello    World\nprint(text.expandtabs(4))  # Hello   World (tab size 4)\n\n# Partition and split with maxsplit\ndata = \"one,two,three,four,five\"\nprint(data.split(\",\", 2))  # ['one', 'two', 'three,four,five']\n\n# Partition (returns tuple)\nurl = \"https://example.com/page\"\nprint(url.partition(\"://\"))  # ('https', '://', 'example.com/page')\n\n# rsplit and rpartition\npath = \"home/user/documents/file.txt\"\nprint(path.rsplit(\"/\", 1))   # ['home/user/documents', 'file.txt']\nprint(path.rpartition(\"/\"))  # ('home/user/documents', '/', 'file.txt')\n\n# Translation table (for character replacement)\ntrans = str.maketrans(\"aeiou\", \"12345\")\ntext = \"Hello World\"\nprint(text.translate(trans))  # H2ll4 W4rld\n\n# Remove prefix/suffix (Python 3.9+)\nfilename = \"document.pdf\"\nprint(filename.removesuffix(\".pdf\"))  # document\nprint(filename.removeprefix(\"doc\"))   # ument.pdf\n\n# String to list and back\nword = \"Python\"\nchars = list(word)  # ['P', 'y', 't', 'h', 'o', 'n']\nprint(''.join(chars))  # Python (back to string)",
      note: "The translate() method is more efficient than replace() when performing multiple character substitutions. The removesuffix() and removeprefix() methods are Python 3.9+ features."
    },
    {
      heading: "String Concatenation and Performance",
      body: "Understanding the performance implications of different string concatenation methods is important for efficient code.",
      code: "# Method 1: Using + operator (slow for many concatenations)\nresult = \"\"\nfor i in range(5):\n    result += str(i) + \" \"\nprint(result)  # 0 1 2 3 4 \n\n# Method 2: Using join() (most efficient for multiple strings)\nparts = []\nfor i in range(5):\n    parts.append(str(i))\nresult = \" \".join(parts)\nprint(result)  # 0 1 2 3 4\n\n# Method 3: List comprehension with join\nnumbers = [str(i) for i in range(5)]\nresult = \" \".join(numbers)\nprint(result)  # 0 1 2 3 4\n\n# Method 4: StringIO (for very large strings)\nfrom io import StringIO\n\noutput = StringIO()\nfor i in range(5):\n    output.write(str(i) + \" \")\nresult = output.getvalue()\nprint(result)  # 0 1 2 3 4 \n\n# Performance comparison (conceptual)\n# join() is generally the fastest for multiple strings\n# + operator creates new string objects each time\n\n# Building strings with f-strings\ndef build_message(name, items, total):\n    return f\"Customer: {name}\\nItems: {len(items)}\\nTotal: ${total:.2f}\"\n\nprint(build_message(\"Alice\", [\"item1\", \"item2\"], 99.99))",
      note: "For concatenating many strings, use join() instead of +. The + operator creates a new string object for each concatenation, leading to O(n²) complexity, while join() is O(n)."
    },
    {
      heading: "Raw Strings and Escape Sequences",
      body: "Escape sequences allow you to include special characters in strings. Raw strings treat backslashes as literal characters.",
      code: "# Common escape sequences\nprint(\"Hello\\nWorld\")    # Newline\nprint(\"Hello\\tWorld\")    # Tab\nprint(\"Hello\\\"World\\\"\")  # Double quote\nprint(\"Hello\\'World\\'\")  # Single quote\nprint(\"C:\\\\Users\\\\John\") # Backslash\nprint(\"Hello\\x41\")       # Hex character (A)\n\n# Raw strings (r prefix) - ignore escape sequences\npath = r\"C:\\Users\\John\\Documents\"\nprint(path)  # C:\\Users\\John\\Documents\n\n# Raw string with regex\nimport re\npattern = r\"\\d+\"  # Raw string for regex pattern\ntext = \"123 abc 456\"\nprint(re.findall(pattern, text))  # ['123', '456']\n\n# Multiline strings with escape characters\ntext = \"This is line 1\\\nThis is line 2\\\nThis is line 3\"\nprint(text)\n\n# Unicode characters\nprint(\"\\u00A9\")  # © (copyright symbol)\nprint(\"\\u03A9\")  # Ω (Omega)\nprint(\"\\U0001F600\")  # 😀 (emoji)\n\n# Combining raw strings with triple quotes\npath = r\"\"\"This is a\nraw multiline\nstring with \\backslashes\\\"\"\"\nprint(path)",
      note: "Raw strings are especially useful for file paths and regular expressions. They prevent Python from interpreting backslashes as escape sequences."
    },
    {
      heading: "String Comparison and Sorting",
      body: "Strings can be compared lexicographically (dictionary order) and sorted using various methods.",
      code: "# String comparison (lexicographic)\nprint(\"apple\" == \"apple\")  # True\nprint(\"apple\" != \"orange\") # True\nprint(\"apple\" < \"banana\")  # True (a comes before b)\nprint(\"Apple\" < \"apple\")   # True (uppercase letters come before lowercase)\nprint(\"10\" < \"2\")         # True (string comparison, not numeric)\n\n# Case-insensitive comparison\ndef compare_ignore_case(str1, str2):\n    return str1.lower() == str2.lower()\n\nprint(compare_ignore_case(\"Hello\", \"HELLO\"))  # True\n\n# Sorting strings\nfruits = [\"banana\", \"Apple\", \"cherry\", \"date\"]\nprint(sorted(fruits))                  # ['Apple', 'banana', 'cherry', 'date']\nprint(sorted(fruits, key=str.lower))   # ['Apple', 'banana', 'cherry', 'date'] (case-insensitive)\nprint(sorted(fruits, reverse=True))    # ['date', 'cherry', 'banana', 'Apple']\n\n# Natural sort (handles numbers properly)\nimport re\n\ndef natural_sort_key(s):\n    return [int(c) if c.isdigit() else c.lower() for c in re.split('(\\d+)', s)]\n\nitems = [\"item10\", \"item2\", \"item1\", \"item20\"]\nprint(sorted(items))                         # ['item1', 'item10', 'item2', 'item20']\nprint(sorted(items, key=natural_sort_key))   # ['item1', 'item2', 'item10', 'item20']\n\n# String ordering based on locale\nimport locale\nlocale.setlocale(locale.LC_ALL, 'en_US.UTF-8')\nwords = [\"café\", \"cafe\", \"apple\", \"banana\"]\nprint(sorted(words, key=locale.strxfrm))  # Locale-aware sorting",
      note: "String comparison in Python is case-sensitive and uses Unicode code points. For natural sorting of strings containing numbers, use regular expressions to extract and compare numeric parts."
    },
    {
      heading: "Working with Substrings",
      body: "Python offers various ways to extract, modify, and work with substrings within larger strings.",
      code: "# Extracting substrings\ntext = \"Hello, World! Python Programming\"\n\n# Extract using slicing\nprint(text[0:5])      # Hello\nprint(text[7:12])     # World\n\n# Extract using split\nprint(text.split(\" \")[2])  # Python\n\n# Get prefix and suffix\nprefix = text[:text.index(\",\")]  # Hello\nsuffix = text[text.index(\",\")+2:] # World! Python Programming\nprint(prefix, suffix)\n\n# Check if substring exists\nprint(\"Python\" in text)   # True\nprint(\"Java\" in text)     # False\n\n# Find all occurrences of a substring\nimport re\npattern = r\"o\"  # Regular expression for letter 'o'\nmatches = re.finditer(pattern, text)\npositions = [match.start() for match in matches]\nprint(positions)  # [4, 8, 19, 26]\n\n# Extract text between delimiters\ns = \"<title>My Page</title>\"\nstart = s.find(\"<title>\") + len(\"<title>\")\nend = s.find(\"</title>\")\ntitle = s[start:end]\nprint(title)  # My Page\n\n# Extract text using regular expressions\nimport re\nhtml = \"<p>Paragraph 1</p><p>Paragraph 2</p>\"\nparagraphs = re.findall(r\"<p>(.*?)</p>\", html)\nprint(paragraphs)  # ['Paragraph 1', 'Paragraph 2']\n\n# Replace substrings\ntext = \"Hello, World!\"\nnew_text = text.replace(\"World\", \"Python\")\nprint(new_text)  # Hello, Python!\n\n# Replace multiple substrings\ndef replace_multiple(text, replacements):\n    for old, new in replacements.items():\n        text = text.replace(old, new)\n    return text\n\nreplacements = {\n    \"Hello\": \"Hi\",\n    \"World\": \"there\",\n    \"!\": \".\"\n}\nprint(replace_multiple(\"Hello, World!\", replacements))  # Hi, there.",
      note: "For complex substring operations, consider using regular expressions. They provide powerful pattern matching capabilities beyond simple substring operations."
    },
    {
      heading: "String Encoding and Decoding",
      body: "Understanding string encoding is crucial for working with international text and file I/O operations.",
      code: "# String to bytes (encoding)\ntext = \"Hello, World!\"\nbyte_data = text.encode(\"utf-8\")\nprint(byte_data)  # b'Hello, World!'\n\n# Bytes to string (decoding)\ndecoded_text = byte_data.decode(\"utf-8\")\nprint(decoded_text)  # Hello, World!\n\n# Different encodings\ntext = \"Café\"\nprint(text.encode(\"utf-8\"))    # b'Caf\\xc3\\xa9'\nprint(text.encode(\"latin-1\"))  # b'Caf\\xe9'\nprint(text.encode(\"ascii\", errors=\"replace\"))  # b'Caf?'\n\n# Handling Unicode characters\nunicode_text = \"你好，世界！\"\nencoded = unicode_text.encode(\"utf-8\")\nprint(encoded)  # b'\\xe4\\xbd\\xa0\\xe5\\xa5\\xbd\\xef\\xbc\\x8c\\xe4\\xb8\\x96\\xe7\\x95\\x8c\\xef\\xbc\\x81'\nprint(encoded.decode(\"utf-8\"))  # 你好，世界！\n\n# Reading files with encoding\n# with open('file.txt', 'r', encoding='utf-8') as f:\n#     content = f.read()\n\n# Writing files with encoding\n# with open('file.txt', 'w', encoding='utf-8') as f:\n#     f.write(\"Hello, World!\")\n\n# Normalizing Unicode text\nimport unicodedata\ntext = \"Café\"\nnormalized = unicodedata.normalize(\"NFKD\", text)\nprint(ascii(normalized))  # 'Cafe\\u0301'\n\n# Character detection\ndef detect_encoding(bytes_data):\n    try:\n        decoded = bytes_data.decode(\"utf-8\")\n        return \"utf-8\", decoded\n    except UnicodeDecodeError:\n        try:\n            decoded = bytes_data.decode(\"latin-1\")\n            return \"latin-1\", decoded\n        except:\n            return \"unknown\", None\n\nsample = b'Hello, Caf\\xe9'\nencoding, text = detect_encoding(sample)\nprint(f\"Encoding: {encoding}, Text: {text}\")",
      note: "UTF-8 is the most common encoding for web and international text. Always specify encoding when reading or writing files to avoid platform-dependent issues."
    },
    {
      heading: "String Interpolation and Templates",
      body: "Python offers several ways to interpolate variables into strings, from simple f-strings to template strings for user-controlled formatting.",
      code: "# f-strings with expressions\nname = \"Alice\"\nage = 25\nprint(f\"{name.upper()} is {age + 5} years old in 5 years\")\nprint(f\"{'Hello' + ' ' + 'World'}\")\n\n# Template strings (safer for user input)\nfrom string import Template\n\ntemplate = Template(\"Hello, $name! Your total is $amount\")\nresult = template.substitute(name=\"Alice\", amount=99.99)\nprint(result)  # Hello, Alice! Your total is 99.99\n\n# Template with dictionary\nuser = {\"name\": \"Bob\", \"amount\": 49.95}\nprint(template.substitute(user))  # Hello, Bob! Your total is 49.95\n\n# Safe substitute (won't raise error for missing values)\ntemplate2 = Template(\"$name has $count items\")\nprint(template2.safe_substitute(name=\"Alice\"))  # Alice has $count items\n\n# Advanced template usage\nclass CustomTemplate(Template):\n    delimiter = '#'  # Custom delimiter\n\ntemplate3 = CustomTemplate(\"Hello, #name!\")\nprint(template3.substitute(name=\"Alice\"))  # Hello, Alice!\n\n# Format string with dictionary\ndata = {\"name\": \"Alice\", \"age\": 25}\nprint(\"Name: {name}, Age: {age}\".format(**data))  # Name: Alice, Age: 25\n\n# Formatting with alignment and padding\nprint(\"{:<10} {:>10}\".format(\"Left\", \"Right\"))  # Left           Right\nprint(\"{:^20}\".format(\"Center\"))  #       Center",
      note: "Template strings are useful when the template is provided by users or needs to be parsed from configuration files. They're safer than f-strings for dynamic content."
    },
    {
      heading: "Practical Examples - Real-World String Operations",
      body: "Here are some practical examples of string manipulation in real-world scenarios.",
      code: "# Example 1: Email validation\ndef validate_email(email):\n    if '@' not in email or '.' not in email:\n        return False\n    local, domain = email.split('@', 1)\n    if not local or not domain:\n        return False\n    if '.' not in domain:\n        return False\n    if '..' in local or '..' in domain:\n        return False\n    return True\n\nprint(validate_email(\"john@example.com\"))  # True\nprint(validate_email(\"invalid-email\"))     # False\n\n# Example 2: URL parsing\ndef parse_url(url):\n    if not url.startswith(('http://', 'https://')):\n        url = 'https://' + url\n    \n    parts = url.split('://', 1)\n    protocol = parts[0]\n    rest = parts[1] if len(parts) > 1 else url\n    \n    if '/' in rest:\n        domain, path = rest.split('/', 1)\n        path = '/' + path\n    else:\n        domain = rest\n        path = '/'\n    \n    if ':' in domain:\n        domain, port = domain.split(':', 1)\n    else:\n        port = None\n    \n    return {\n        'protocol': protocol,\n        'domain': domain,\n        'port': port,\n        'path': path\n    }\n\nprint(parse_url(\"example.com/page\"))\nprint(parse_url(\"https://example.com:8080/page\"))\n\n# Example 3: Text cleaning\ndef clean_text(text):\n    # Remove extra whitespace\n    text = ' '.join(text.split())\n    # Remove special characters\n    import re\n    text = re.sub(r'[^\\w\\s]', '', text)\n    # Convert to lowercase\n    return text.lower()\n\nsample = \"  Hello,   World!  This is a TEST.  \"\nprint(clean_text(sample))  # hello world this is a test\n\n# Example 4: CSV parsing\ndef parse_csv_line(line):\n    # Split by comma, handling quoted fields\n    fields = []\n    current = []\n    in_quotes = False\n    \n    for char in line:\n        if char == '\"':\n            in_quotes = not in_quotes\n        elif char == ',' and not in_quotes:\n            fields.append(''.join(current).strip())\n            current = []\n        else:\n            current.append(char)\n    \n    fields.append(''.join(current).strip())\n    return fields\n\ncsv_line = '\"Alice\",25,\"New York\",\"Engineer\"'\nprint(parse_csv_line(csv_line))  # ['Alice', '25', 'New York', 'Engineer']\n\n# Example 5: Password generator\nimport random\nimport string\n\ndef generate_password(length=12):\n    chars = string.ascii_letters + string.digits + \"!@#$%^&*\"\n    return ''.join(random.choice(chars) for _ in range(length))\n\nprint(generate_password())  # Random password\n\n# Example 6: Slug generation (URL-friendly strings)\ndef slugify(text):\n    # Convert to lowercase\n    text = text.lower()\n    # Replace spaces with hyphens\n    text = text.replace(' ', '-')\n    # Remove special characters\n    import re\n    text = re.sub(r'[^a-z0-9-]', '', text)\n    # Remove consecutive hyphens\n    text = re.sub(r'-+', '-', text)\n    # Remove leading/trailing hyphens\n    return text.strip('-')\n\nprint(slugify(\"Hello World! Welcome to Python Programming\"))\n# hello-world-welcome-to-python-programming",
      note: "These practical examples demonstrate common string manipulation tasks: validation, parsing, cleaning, and transformation. Always consider edge cases when working with real-world data."
    }
  ]
}